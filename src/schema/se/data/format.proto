syntax = "proto3";

package se.data;

// A data format that can be edited by the editor.
enum Format {
  // The format is unknown or not specified.
  UNKNOWN = 0;
  // The Markdown format, using the [CommonMark](http://commonmark.org/) standard.
  MARKDOWN = 1;
}
